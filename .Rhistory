# Hvor god er modellen
drop1(analysis, test="Chisq")
analysis<-glm(events ~ conc + age + gender + age*gender,family=poisson(link=log),data=blad,offset=log(at.risk))
summary(analysis)
# Hvor god er modellen
drop1(analysis, test="Chisq")
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2)+I((age*gender)^2))
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
summary(analysis2)
analysis2 <- update(analysis2, ~.+I((age)^3))
summary(analysis2)
# Hvor god er modellen
drop1(analysis2, test="Chisq")
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
summary(analysis2)
# Hvor god er modellen
drop1(analysis2, test="Chisq")
analysis2 <- update(analysis2, ~.+I((age)^3)+I((conc)^2))
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
analysis2 <- update(analysis2, ~.+I((age)^3)+I((conc)^3))
summary(analysis2)
# Hvor god er modellen
drop1(analysis2, test="Chisq")
analysis2 <- update(analysis2, ~.+I((age)^3))
# Hvor god er modellen
drop1(analysis2, test="Chisq")
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
analysis2 <- update(analysis2, ~.+I((age)^3))
# Hvor god er modellen
drop1(analysis2, test="Chisq")
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
summary(analysis2)
# Hvor god er modellen
drop1(analysis2, test="Chisq")
analysis<-glm(events ~ s(conc) + s(age) + gender + s(age,gender),family=poisson(link=log),data=blad,offset=log(at.risk))
summary(analysis)
analysis<-glm(events ~ s(conc) + s(age) + gender + s(age,gender),family=poisson(link=log),data=blad,offset=log(at.risk))
analysis<-glm(events ~ s(conc) + s(age) + gender + s(age),family=poisson(link=log),data=blad,offset=log(at.risk))
analysis<-glm(events ~ conc + age + gender + age,gender,family=poisson(link=log),data=blad,offset=log(at.risk))
analysis<-glm(events ~ conc + age + gender + age,family=poisson(link=log),data=blad,offset=log(at.risk))
summary(analysis)
analysis<-glm(events ~ conc + age + gender + age*gender,family=poisson(link=log),data=blad,offset=log(at.risk))
anders <- gam(events~gender+s(age)+s(age,gender)+s(conc)+s(age,conc)+offset(I(log(at.risk))),family=poisson(link = "log"),data=blad)
head(blad)
analysis<-glm(events ~ conc + age + gender + age*gender,family=poisson(link=log),data=blad,offset=log(at.risk))
summary(analysis)
analysis<-glm(events ~ conc + age  + age*gender,family=poisson(link=log),data=blad,offset=log(at.risk))
summary(analysis)
analysis<-glm(events ~ conc + age +gender,family=poisson(link=log),data=blad,offset=log(at.risk))
summary(analysis)
summary(anders)
# Hvor god er modellen
drop1(analysis, test="Chisq")
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
summary(analysis2)
analysis2 <- update(analysis2, ~.+I((age)^3)+I(conc^2))
summary(analysis2)
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
analysis2 <- update(analysis2, ~.+I((age)^3)+I(conc^2))
summary(analysis2)
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
analysis2 <- update(analysis2, ~.+I((age)^3)+I(conc^3))
summary(analysis2)
analysis2 <- update(analysis2, ~.+I(conc^3))
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
analysis2 <- update(analysis2, ~.+I(conc^3))
summary(analysis2)
# Taylor udvider til ny analysis
analysis2 <- update(analysis, ~.+I((age)^2)+I((conc)^2))
analysis2 <- update(analysis2, ~.+I(conc^3))
analysis2 <- update(analysis2, ~.+I(conc^4))
analysis2 <- update(analysis2, ~.+I(conc^5))
analysis2 <- update(analysis2, ~.+I(conc^6))
analysis2 <- update(analysis2, ~.+I(conc^7))
summary(analysis2)
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad[c(175:220), ]
head(blad)
# Antal observationer
N <- length(blad$events)
# p.hat (empirisk sandsynlighed)
p.hat <- sum(blad$events/N)
max(blad$conc)
analysis<-glm(events ~ conc + age + gender,family=poisson(link=log),data=blad,offset=log(at.risk))
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age,by=female)+s(age)+s(conc)+
s(age,conc)+offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# Hvor god er modellen
drop1(analysis, test="Chisq")
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis2,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 100
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis2,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 100
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis2$residuals)
plot(analysis$residuals)
# AIC
AIC(analysis)
N
analysis <- gam(events~s(age)+s(conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis2,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 100
analysis <- gam(events~s(age)+s(conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(conc)+s(age,by=female)
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~gender+s(age)+s(conc)+s(age,by=female)
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~gender+s(age)+s(conc)+s(age,by=female)
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~gender+s(age)+s(conc)+s(age,by=female)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis2,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 100
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis2,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 100
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(exp(conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~gender+s(age)+s(exp(conc/1000))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age)+s(exp(conc/1000))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s((conc))+s(age,by=female)+s(age,conc)+s(conc,by=female)
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~gender+s(age)+s((conc))+s(age,by=female)+s(age,conc)+s(conc,by=female)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age)+s((conc))+s(age,by=female)+s(age,conc)+s(conc,by=female)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=male)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
plot(analysis$residuals[1:100])
plot(analysis$residuals)
maxr <- 100
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
maxr <- 10
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~gender+s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
