blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis2,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
plot(analysis$residuals,ylim=c(0,2))
plot(analysis$residuals,ylim=c(0,5))
plot(analysis$residuals,ylim=c(0,10))
plot(analysis$residuals,ylim=c(0,100))
plot(analysis$residuals,ylim=c(0,5))
plot(analysis$residuals,ylim=c(0,20))
manygam((events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad))
manygam((events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+offset(I(log(at.risk))),family=poisson(link = "log"),data=blad))
manygam(events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+offset(I(log(at.risk))),family=poisson(link = "log"),data=blad)
library("mvabund")
install.packages("mvabund")
library("mvabund")
manygam(events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+offset(I(log(at.risk))),family=poisson(link = "log"),data=blad)
library("mgcv")
# Asger wd
setwd("/Users/AsgerSturisTang/OneDrive - Danmarks Tekniske Universitet/DTU/4. Semester/Arsenik i GIT/Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female)+s(age,conc)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
plot(analysis)
AIC(analysis)
plot(analysis)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
maxr <- 607
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred, digits=2), lun$events[order(lun$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr), xlab="Predicted events", ylab="Actual events")
lines(0:maxr, 0:maxr, type="l")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
maxr <- 607
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred, digits=2), lun$events[order(lun$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr), xlab="Predicted events", ylab="Actual events")
library(mgcv)
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
fblad <- read.table("fblad.sw.dat", header=TRUE)
library("mgcv")
# Asger wd
setwd("/Users/AsgerSturisTang/OneDrive - Danmarks Tekniske Universitet/DTU/4. Semester/Arsenik i GIT/Data")
set.seed(69)
fblad <- read.table("fblad.txt", header=TRUE)
mblad <- read.table("mblad.txt", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.txt", header=TRUE)
mblad <- read.table("mblad.txt", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals, ylim=c(-10, 20))
plot(analysis$fitted.values, analysis$residuals)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.txt", header=TRUE)
mblad <- read.table("mblad.txt", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals, ylim=c(-10, 20))
plot(analysis$fitted.values, analysis$residuals)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
plot(analysis$residuals, ylim=c(-10, 20))
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
plot(analysis)
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
plot(analysis$residuals, ylim=c(-10, 20))
plot(analysis$fitted.values, analysis$residuals)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
plot(analysis)
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
plot(analysis$residuals, ylim=c(-10, 20))
plot(analysis$fitted.values, analysis$residuals)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
summary(analysis)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
plot(analysis)
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
maxr <- 150
# Laver foreløbig test, tror det her er den rigtige måde at plotte det på
plot(round(prediction.data.original$pred,2),blad$events[order(blad$events, decreasing = TRUE)], xlim=c(0, maxr), ylim=c(0, maxr))
lines(0:maxr,0:maxr, type="l")
v = vector()
x = vector()
blad$events<- blad$events[order(blad$events, decreasing = TRUE)]
for (i in 1:length(prediction.data.original$pred)){
res <- mean(blad$events[0.1*(i-1) <= prediction.data.original$pred & prediction.data.original$pred < 0.1*i])
v = c(v,res)
x = c(x, 0.1*i)
}
sd.Pred <- sd(prediction.data.original$pred)
length(prediction.data.original$upper)
plot(x, v, xlim=c(0, maxr), ylim = c(0,maxr))
lines(0:maxr,0:maxr, type="l")
lines(0:maxr+sd.Pred,0:maxr, type="l", col = "red")
lines(0:maxr-sd.Pred,0:maxr, type="l", col = "red")
plot(analysis$residuals)
plot(analysis$residuals, ylim=c(-10, 20))
plot(analysis$fitted.values, analysis$residuals)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
plot(analysis$fitted.values, analysis$residuals)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
library("mgcv")
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
plot(analysis)
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(age,village1)
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
# Frederik wd
setwd("C:\\Users\\frede\\OneDrive\\Dokumenter\\DTU\\4. Semester\\Fagprojekt\\ArsenikGit\\Data")
set.seed(69)
fblad <- read.table("fblad.sw.dat", header=TRUE)
mblad <- read.table("mblad.sw.dat", header=TRUE)
fblad$gender <- "Female"
mblad$gender <- "Male"
fblad$female <- 1
mblad$female <- 0
blad <- rbind(fblad,mblad)
blad$village1 <- 1*(blad$group == 1)
N <- length(blad$events)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(age,village1)
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(age,village1)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(age,by=village1)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(conc,by=village1)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
summary(analysis)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(age,by=village1)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
summary(analysis)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 + s(log(1+conc),by=village1)+
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
summary(analysis)
analysis <- gam(events~s(age)+s(log(1+conc))+s(age,by=female) + gender*village1 +
offset(I(log(at.risk))),
family=poisson(link = "log"),
data=blad)
# AIC
AIC(analysis)
summary(analysis)
# Laver signifikans niveauer
prediction.temp<-as.data.frame(predict(analysis,se.fit=T))
prediction.data<-data.frame(pred=prediction.temp$fit, upper=prediction.temp$fit+ 1.96*prediction.temp$se.fit, lower=prediction.temp$fit-1.96*prediction.temp$se.fit)
prediction.data.original <- exp(prediction.data)
prediction.data.original <- prediction.data.original[order(blad$events, decreasing = TRUE),]
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
gam.check(analysis)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group, xlim=c(0,10))
summary(analysis)
gam2objective(analysis)
analysis$smooth
analysis$smooth[1][1]
analysis$coefficients
analysis$linear.predictors
analysis$weights
analysis$y
summary(analysis)
analysis$fitted.values>analysis$conc==0
analysis$fitted.values>analysis$conc=0
analysis$fitted.values>0
analysis$fitted.values
analysis$optimizer
### Create basis functions
B <- bs(x, degree=3, intercept=TRUE, Boundary.knots=c(0, 6.2), knots=c(1.55, 3.10, 4.65))
analysis
plot(analysis$fitted.values, (blad$events - analysis$fitted.values)/sqrt(analysis$fitted.values),col=blad$group)
analysis$fitted.values
analysis$fitted.values>10
analysis$fitted.values[analysis$fitted.values>10]
